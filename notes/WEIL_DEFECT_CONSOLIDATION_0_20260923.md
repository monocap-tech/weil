# WEIL-DEFECT-CONSOLIDATION-0
## Proof inventory and dependency DAG
Date: 2026-09-23

## Determination

The accumulated work supports a theorem-bearing **Weil-defect subsystem** that remains meaningful after deleting the final RH-closing arrows.

```math
\boxed{
\text{Weil-defect theory}
+
\text{actual-zeta exclusion theorem}
\Longrightarrow
\text{RH application}.
}
```

The present checkpoint records the theorem inventory before axiom minimization.

## Retained components

1. Finite negative-index structure: **imported theorem + exact specialization**.
2. Finite-rank / rank-one defect structure: **derived**.
3. Critical-line tail monotonicity: **proved**.
4. Spectral screening distinction: **structural**.
5. Normalized persistent negativity: **conditional on persistence**.
6. Selected quartet zero-moment law: **proved in model**.
7. $O(z^{-2})$ far decay: **proved**.
8. Weighted next-jet identification: **exact reduction**.
9. Stronger source-free moment law: **not obtained**.
10. Neutral relation $W_ck=0$: **conditional on neutral mode**.
11. Fixed-window log-order + finite-shift operator: **derived**.
12. Quasianalytic bootstrap from finite prime translations: **not obtained**.

## Negative branch

Assume a persistent negative ray

```math
y=(a,u),
\qquad
\kappa=\|u\|^2-\|a\|^2>0.
```

Then normalized selected negativity tends to a nonzero negative constant, and the full Weil form obeys

```math
\frac{Q_W(g_n)}{\varepsilon_n^2}
=
[z_n,z_n]_{\mathcal J}-\|b_n\|^2.
```

The selected raw residue vector satisfies

```math
\mathbf{1}^Tv=0,
```

hence

```math
R_v(z)=O(|z|^{-2}).
```

The unresolved finite/intermediate complement is

```math
\mathcal N_v[\psi]
=
\sum_{\mu\notin F}^{\rm near}
m_\mu\psi(\mu)
\frac{H_v^{(m_\mu)}(\mu)}
{\Xi^{(m_\mu)}(\mu)}.
```

Current open gate:

```math
\boxed{\texttt{AZ-NEXTJET-LOC}}
```

or equivalently an actual-zeta KPH/transversality floor.

## Neutral branch

For a physical neutral mode,

```math
W_ck=0.
```

At fixed support,

```math
\mathcal W_c
=
\mathcal A_\infty
-
\sum_{\log n<2c}
\frac{\Lambda(n)}{\sqrt n}
(\tau_{\log n}+\tau_{-\log n})
+
\mathcal R_{\rm pole},
```

with

```math
\Psi_c(t)=\log|t|+O_c(1).
```

Current open gate:

```math
\boxed{\texttt{AZ-FIN-WEIL-NULL-EXTENSION}}.
```

## Dependency split

```math
\boxed{
\text{Weil-defect subsystem}
\quad\text{survives without}\quad
\text{RH closure}.
}
```

This is the reason for extracting the project into its own repository.

## Next pass

```math
\boxed{
\texttt{WEIL-DEFECT-CONSOLIDATION-1 / AXIOM MINIMIZATION AND THEOREM EXTRACTION}
}
```

Classify each retained statement as:

1. general Weil/Pontryagin defect theorem;
2. zeta-Weil theorem;
3. RH-facing corollary/open obligation.
